# Blocksy accessibility surfaces

For Blocksy navigation, article templates, search, widgets, and editor-adjacent controls, preserve semantic landmarks, heading order, visible focus, keyboard access, 44px touch targets, text zoom, and screen-reader names. Test the mobile menu open/close path and Blocksy dynamic states instead of relying on a static screenshot.
