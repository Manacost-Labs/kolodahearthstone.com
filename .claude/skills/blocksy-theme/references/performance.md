# Blocksy performance

Measure cold and warm HTML, CSS, JavaScript, image, and font delivery before changing a theme surface. Keep critical CSS scoped, avoid loading editor/admin assets on public requests, preserve lazy-loading and image-optimizer sidecars, and invalidate only the affected page/cache group. Validate LCP, INP, CLS, console errors, and article view counting on staging and each production edge.
